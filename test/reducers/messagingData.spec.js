import assert from 'assert'
import messagginData from '../../src/reducers/messagginData'
import { defaultDeviceState, defaultState } from '../../src/reducers/messagginData'

describe('Testing the messaggingData reducer function', function () {
	it('should properly update the store when a new message comes', function () {
		
	})
})
