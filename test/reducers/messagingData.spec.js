import assert from 'assert'
import { defaultDeviceState, defaultState, messagingData } from '../../src/reducers/messagingData'

describe('Testing the messagingData reducer function', function () {
	it('should properly update the store when a new message arrives.', function () {
		
	})

	it('should properly update the store with new device state.', function () {
		
	})

	it('should properly update the store with new security alert.', function () {
		
	})

	it('should properly rest to default state on user logout.', function () {
		
	})
})
